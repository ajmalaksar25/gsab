# CLAUDE.md

@AGENTS.md

The shared, tool-agnostic project instructions live in **AGENTS.md** (imported above).
Add any new shared convention to `AGENTS.md`, not here — it is the cross-tool standard
(other agents read it directly), and this file only imports it so Claude Code picks it up.

Personal, machine-local notes and preferences live in **CLAUDE.LOCAL.md** (untracked via
`.git/info/exclude`), imported below when present.

@CLAUDE.LOCAL.md
